import { CAR_CRASH_DISTRICT_TYPE, CAR_CRASH_TYPE } from '../../store/CarCrashEventStore/CarCrashEvent/types';


export const carCrashTypeList: Array<CAR_CRASH_TYPE> = Object.values(CAR_CRASH_TYPE);
export const carCrashDistrictList: Array<CAR_CRASH_DISTRICT_TYPE> = Object.values(CAR_CRASH_DISTRICT_TYPE);
